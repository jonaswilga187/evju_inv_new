# Docker Compose / Homeserver
# Kopiere diese Datei nach .env und passe die Werte an.

# Geheimes Secret für JWT-Signatur (unbedingt ändern!)
JWT_SECRET=bitte-ändern

# MongoDB-Passwort (muss mit MONGO_INITDB_ROOT_PASSWORD übereinstimmen)
MONGO_PASSWORD=changeme

# Öffentliche URL, unter der Nutzer die App aufrufen (z. B. deine Nginx-URL).
# Wichtig für CORS und Cookies – exakt die URL deines Nginx verwenden.
FRONTEND_URL=https://inventory.deinedomain.de
